# Local archive

Historical KRAG phase plans and programme artefacts under `planning/` are **not published** in this GitHub repository. Keep them in your local checkout only (see root `.gitignore`).

The live roadmap and contracts remain in `skills_docs/krag/`.
