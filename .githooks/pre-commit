#!/usr/bin/env bash
# Git pre-commit hook — runs the pre-commit framework on staged changes.
set -euo pipefail
ROOT="$(git rev-parse --show-toplevel)"
exec "$ROOT/scripts/dev_workflow/pre_commit_check.sh"
